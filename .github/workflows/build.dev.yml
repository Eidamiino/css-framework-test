name: Development (Slave) build
on:
  push:
    branches:
      - dev
jobs:
  build:
    uses: KeenMate/gha-shared/.github/workflows/deploy-docker-image.yml@master
    secrets: inherit
    with:
      image-name: gcp-documenthub
      image-tag: development
      deployment-host: web-03.km8.vm
      deployment-dir: gcp-documenthub
      npm-script: build:km
